撒西不理

是的，可以直接在 Docker Compose 文件中使用 `depends_on` 来定义服务间的启动顺序。

它的核心用法很简单，可以直接在服务下添加，**主要目的是控制容器的启动顺序**。

### 📝 基础用法：直接添加

在 `docker-compose.yml` 文件中，你可以在需要依赖其他服务的服务下，直接添加 `depends_on` 关键字，并列出它所依赖的服务名称。

下面是一个基本的示例，`web` 服务依赖于 `db` 和 `redis` 服务：
```yaml
services:
  web:
    build: .
    depends_on:
      - db
      - redis
  redis:
    image: redis
  db:
    image: postgres
```
在这个例子中，当你执行 `docker compose up` 时，Compose 会先启动 `db` 和 `redis`，最后才启动 `web` 服务。

### ⚠️ 重要注意事项

使用 `depends_on` 时，有两个关键点需要了解：

1.  **只保证启动顺序，不等待服务就绪**：`depends_on` **只保证依赖的容器已经启动（状态变为"running"）**，**但不会等待容器内的服务（如数据库）完全初始化并准备好接收连接**。如果 `web` 服务在 `db` 数据库完全启动前就尝试连接，依然可能会失败。

2.  **版本兼容性**：`depends_on` 是 Docker Compose 文件的核心功能，在较新版本（如 Compose Specification）和旧版本（如 2.1+）中都得到支持。不过，如果你使用的是非常古老的 **Compose File Version 3**，直接使用 `depends_on` 可能会遇到问题。建议检查你的 Compose 文件版本，并尽可能使用最新的 Compose Specification。

### 🚀 高级用法：条件等待 (Condition)

为了解决“等待服务就绪”的问题，Docker Compose 提供了更高级的 `condition` 选项。这需要配合服务的 `healthcheck`（健康检查）一起使用。

`depends_on` 支持以下几种 `condition`：

*   **`service_started`**：默认行为，只等待容器启动。
*   **`service_healthy`**：**会等待依赖的容器通过其定义的 `healthcheck`，变为“健康”状态后，再启动当前服务**。这是确保服务真正可用的推荐方式。
*   **`service_completed_successfully`**：等待依赖的容器成功完成其任务并退出（exit code 为 0）。这通常用于一次性任务，比如数据库迁移脚本。

**示例：等待数据库健康**

下面的例子中，`web` 服务会等待 `db` 服务通过健康检查（`pg_isready` 命令）后才会启动。
```yaml
services:
  web:
    build: .
    depends_on:
      db:
        condition: service_healthy
    restart: true
  db:
    image: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      retries: 5
      start_period: 30s
      timeout: 10s
```
**注意**：使用 `condition` 的“长语法”格式时，`depends_on` 的值是一个对象（或对象列表），而不是简单的字符串数组。

# 1.VibeCoding

## 1.简介 

### 1.核心

不可替代的特点:背锅，人比大模型便宜

Vibe Coding 不是只会写 Prompt。它至少包括以下几种能力：

- **表达能力**：能把需求说清楚
- **拆解能力**：能把大目标拆成小任务
- **判断能力**：能判断 AI 方案是否靠谱
- **调试能力**：能定位问题并持续修正
- **承担责任的能力**: 能保证项目可运行、可维护、可上线
- **对外沟通的能力**: 能够对外阐述、沟通、协商当前项目的相关情况

### 2.发展

24:打开网页，问gpt

25年中:cluade code

面试中不可或缺

### 3.误区

误区一：Vibe Coding 等于不会代码也能做一切

误区二：Prompt 写得越长越好

误区三：AI 生成的代码可以直接相信

## 2.AI 编程工具全景

| 工具        | 适合场景                         | 特点                                         |
| ----------- | -------------------------------- | -------------------------------------------- |
| Codex       | 终端开发、代码库修改、自动化任务 | 更像一个工程搭档，可以读项目、改文件、跑命令 |
| Cursor      | IDE 内编码、补全、重构、聊天     | 对新手友好，适合边看代码边对话               |
| Claude Code | 长上下文项目分析、Agent 工作流   | 适合复杂项目理解与修改                       |

cursor:优点:直接下载使用，支付宝支付；缺点:灵活性低（忽略）

claude code:模型能力非常强(claude模型)，缺点:贵，国内用不了,支付麻烦

Trae,WorkBuddy,Qoder

前两个软件有国际版:可以用国外模型，没有claude

codex:模型很强(openAI),支付方式麻烦,海外支付方式

------

中转站:

买一大堆gpt账号，搭建号池,公布base_url和key，风险:跑路,模型掺水

### 3.Cursor 的安装配置

Network--->网络改为http1.1

### 4.codex和claude code

vs code 添加插件，ccswitch

### 5.trae

10$一个月，可以薅羊毛

## skill

| 平台          | 地址                          | 类型 | 特色                                              |
| ------------- | ----------------------------- | ---- | ------------------------------------------------- |
| **skills.sh** | https://skills.sh             | 国外 | Vercel 官方推荐的发现平台                         |
| **SkillsMP**  | https://skillsmp.com/zh       | 国外 | 最大的 Skill 市场，支持中文界                     |
| **skillhub**  | https://skillhub.cn           | 国内 | 国内腾讯系平台，国内应用生态友好                  |
| **skill-cn**  | https://www.skill-cn.com/     | 国内 | 国内skill市场，注重分享使用心得                   |
| **lobehub**   | https://lobehub.com/zh/skills | 国外 | 虽然是国外平台，但是中文友好，分类齐全，skill众多 |

上这些网站找skills,

### 2.prompt

模板

- 背景：现在在做什么项目
- 目标：希望 AI 完成什么
- 约束：不能做什么，必须遵守什么
- 验证：完成后如何确认正确

> 注意：不要说`请`>提示词稀释，重要信息放在消息首位段，
>
> 给大模型设置一个角色，
>
> 测序模型是否降智商--->每次回答问题，叫我一声兄弟

需求拆解，技术方案，debug，重构





qwen-max



# 2.项目部署

这是一个非常切中当下程序员核心焦虑的好问题。在AI时代，对于**大多数普通程序员**来说，如果必须在广度和深度之间二选一，我的结论非常明确：

**侧重“广度”是当下环境的最优解，但这个“广度”不是“什么都会一点”，而是“端到端的系统认知广度”。**

---

### 为什么“深度”在这个时代的性价比在降低？

1. **AI正在快速填平纯技术深度的壁垒**
   - 过去，熟练掌握某一门语言/框架的“黑魔法”（比如深入V8引擎、精通Spring源码）是高级程序员的护城河。
   - 但现在，AI对公开的、结构化的技术文档学习能力极强。你花一周啃下来的源码细节，AI可能在几秒内就能总结出关键逻辑并提供最佳实践。
   - **结论**：在纯代码/API层面的“深度钻研”，ROI（投资回报率）正在急剧下降。

2. **“广度”带来的是AI无法替代的上下文判断力**
   - AI擅长在给定的框框里写出完美的代码，但它**缺乏对业务全局、团队协作、系统边界的真实感知**。
   - 广度让你知道：这个需求到底该不该做？应该放在前端还是后端？数据是走缓存还是实时查询？这个方案上线后运维成本有多高？
   - 这些“权衡能力”**无法通过训练模型获得**，因为它依赖于对现实世界的理解。

---

### AI时代，“广度”到底应该广在哪里？

不是让你去学十个编程语言或二十个框架，而是构建一个**“端到端的系统地图”**：

| 广度维度         | 具体内容                               | 为什么重要                                               |
| :--------------- | :------------------------------------- | :------------------------------------------------------- |
| **业务理解能力** | 需求拆解、用户痛点分析、价值判断       | AI能写代码，但只有你能判断该不该写这段代码。             |
| **架构设计思维** | 模块划分、接口设计、技术选型、性能评估 | 解决“代码放在哪里才合理”的问题，而非“代码怎么写”。       |
| **工程化与交付** | CI/CD、监控告警、日志排查、灰度发布    | 代码写完只是开始，能稳定跑在线上才是价值所在。           |
| **跨角色协作**   | 产品、测试、运维、运营的沟通与对齐     | 技术决策往往取决于非技术因素，懂沟通的程序员更有话语权。 |

---

### 那“深度”就彻底不练了吗？—— 不，但做法变了

在这个策略下，你的“深度”不再是**死磕源码**，而是变成**“深度使用AI来扩充广度”**：

- 遇到具体的库或算法，直接让AI帮你深挖细节，**把AI当作你的“深度外挂大脑”**。
- 你只需理解**“它能做什么”**和**“什么场景下不适合”**，而无需记住每一行底层实现。
- 这样，你就能把省下来的精力，投入到上述“广度”的四个维度中去。

---

### 总结：普通程序员的AI时代最优解

**最优解 = 以“广度”为地基（端到端认知） + 以“AI”为杠杆（外包技术深度）。**

你最终要成为的角色，不再是那个**“唯一能解决这个诡异Bug的人”**（这种角色正被AI取代），而是那个**“唯一能判断这个Bug该不该修、优先级多高、什么方案成本最低的人”**。

**一个可执行的建议**：
- **10%的时间**：快速了解新领域（比如云原生、AI Agent）的架构概览，知道它们能解决什么场景。
- **90%的时间**：深度参与你当前项目的**需求评审、架构设计、线上问题排查**，把这些“代码之外”的环节当作你的主战场。

当你的视角从“这块代码怎么实现”变成“这个系统如何成功交付”，你在这个时代就拥有了不可替代的立足点。如果你对如何具体拓展某个“广度”维度（比如如何学习架构设计）感兴趣，我们可以继续聊。



# 项目部署

## 一、部署

### 1.分cpu和gpu部署

docker一键部署

直接调用API,租服务器，本地机房

### 2.部署gup资源服务

将模型封装为接口，后端直接调用接口

nvidia-smi



部署好模型后:

通过post接口调用:填写url,head,body



## 下午

测试

### 1.应用部署





















# 3.项目评估