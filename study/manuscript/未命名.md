节点二返回:

1.文件内容:md_content

2文件路径md_path

节点三

```python
### 步骤1：初始化数据，获取MD核心信息
组装了图片文件路径：images_dir
md_path--->Path(md_path)对象:md_path_obj
md_content不变,
返回3个
### 步骤2：扫描并筛选MD中引用的图片
组装图片路径:img_path
图片上下文:context
image_file单独的图片

集合的元素是元组--->
返回target_images[(image_file, img_path, context)]
### 步骤3：调用多模态大模型生成图片摘要
传入两个参数，一个文件名doc_stem，步骤二的元组

步骤三里面有2个方法:
通用滑动窗口API速率限制器(抽离为公共工具)
调用vim模型

返回:key单独图片，value为图片摘要
summaries
### 步骤4：上传图片至MinIO，替换MD图片路径并填充摘要
传入参数：doc_stem,targeet_images,md_content,summaries
步骤四分为5个方法
# 1 清理minio目录
# 2 批量上传图片，获得minio的urls
# 3 将摘要和url路径合并
# 4 替换md文件的摘要和路径
正则
# 5 上传minio

### 步骤5：备份并保存新MD文件
略
```
